# ==== Backend ====
JWT_SECRET=change-me
# Comma-separated list
CORS_ORIGINS=https://bhagylaxmi.in,https://www.bhagylaxmi.in

# Optional: overwrite DB on every deploy (DANGER)
# FORCE_SEED_DB=true

# Optional override (defaults to sqlite:////data/luckbuddy.db inside container)
# DATABASE_URL=sqlite:////data/luckbuddy.db
